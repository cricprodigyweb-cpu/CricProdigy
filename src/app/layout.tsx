import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Crikprodigy - Professional Cricket Training Platform",
  description: "Master cricket with expert-led courses, video analysis, and personalized coaching for all skill levels",
  keywords: "cricket training, cricket courses, cricket coaching, batting, bowling, fielding, fitness",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
